{% extends 'base.html.twig' %}

{% block body %}
    <h1>Edit Category</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}

        <button type="submit">Save changes</button>
    {{ form_end(form) }}
{% endblock %}